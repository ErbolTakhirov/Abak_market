# 🍽️ Абак маркет - Умный магазин и WhatsApp бот

Премиальное веб-приложение для заказа готовой еды и продуктов с интегрированной системой поиска, рекомендаций и WhatsApp-ботом.

## ✨ Основные функции

- **💎 Современное меню**: Адаптивный веб-интерфейс с «эффектом стекла» (glassmorphism) и плавной анимацией.
- **🔍 Умный поиск товаров**:
  - Поиск с учетом опечаток (Fuzzy search).
  - Поддержка синонимов (например, "кофэ" -> "кофе").
  - Подсказки при вводе (Autocomplete) с категориями и товарами.
- **🔥 Система рекомендаций**: 
  - Блок популярных товаров на основе просмотров и покупок.
  - Похожие товары на странице продукта (алгоритм на основе категории и цены).
- **📦 Управление каталогом**: Удобная админ-панель Jazzmin для управления товарами, категориями и заказами.
- **🤖 WhatsApp Интеграция**: Прием заказов и взаимодействие с клиентами напрямую через WhatsApp (поддержка Meta Business API).

## 🚀 Быстрый старт

### Локальная разработка

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/ErbolTakhirov/Abak_market.git
   cd Abak_market
   ```

2. **Создайте виртуальное окружение**:
   ```bash
   python -m venv venv
   source venv/Scripts/activate  # Для Windows
   ```

3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте окружение**:
   Создайте файл `.env` в корне проекта (см. пример в `.env.example` если есть, или используйте текущие настройки).
   Убедитесь, что `DEBUG=True` для локальной работы.

5. **Запустите миграции**:
   ```bash
   python manage.py migrate
   ```

6. **Запустите сервер**:
   ```bash
   python manage.py runserver
   ```
   Откройте `http://localhost:8000/catalog/menu/` в браузере.

## 🛠 Технологический стек

- **Backend**: Django 4.2+, Django REST Framework.
- **Frontend**: Vanilla HTML5, CSS3 (Glassmorphism), JavaScript (ES6+).
- **Database**: PostgreSQL (Production) / SQLite (Local).
- **Cache/Queue**: Redis & Celery.
- **Admin UI**: Jazzmin.
- **Deployment**: Render.com.

## 📦 Деплой на Render.com

Проект полностью настроен для деплоя на Render:
1. Подключите ваш GitHub репозиторий к Render.
2. Выберите **Web Service**.
3. Build Command: `./build.sh`
4. Start Command: `gunicorn config.wsgi:application`
5. Добавьте необходимые Environment Variables (`SECRET_KEY`, `DATABASE_URL`, `REDIS_URL` и т.д.) в панели управления Render.

---
© 2024 Абак маркет. Создано для идеального пользовательского опыта.
